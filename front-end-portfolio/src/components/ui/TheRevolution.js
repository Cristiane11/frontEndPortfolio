import React from 'react'

function TheRevolution() {
    return (
      <ThemeProvider theme={theme}>
        <Header/>
        TheRevolution 
      </ThemeProvider>
    );
  }
  
  export default TheRevolution;
  